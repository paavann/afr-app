# === FastAPI Core ===
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.18

# === CAD Processing (OpenCascade) ===
# occwl provides high-level wrappers around OCC
# Install pythonocc-core first via conda: conda install -c conda-forge pythonocc-core
# Then install occwl from github:
occwl @ git+https://github.com/AutodeskAILab/occwl.git
trimesh>=4.0.0

# === ML Inference ===
torch>=2.0.0
pytorch-lightning>=1.9.0
dgl>=0.9.0
torchmetrics>=0.11.0

# === Utilities ===
numpy>=1.24.0
pydantic>=2.0.0
